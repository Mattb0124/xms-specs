# TODO: XMS Ticketing (spec set and build)

**Spec:** this repository (`00-overview`, `01-architecture`, `02-modules`, `03-delivery`)
**Ticket:** to be created in ClickUp space "X Platforms" once the path decision lands (2026-09-07)
**Build order:** [Thirty-Day Build](./THIRTY-DAY-BUILD.md) for the first month (day-by-day, ADR-15), then the [Implementation Plan](./IMPLEMENTATION-PLAN.md) (item ids P<phase>.<sprint>.<n>, dependencies, tests)
**Status legend:** `[ ]` not started, `[~]` in progress, `[x]` done and verified

---

## 0. Spec set (this repo)

- [x] Requirements register generated from the workbook with stable IDs, modules and phases (`00-overview/scripts/build_register.py`)
- [x] Product vision, glossary, decision log, roadmap, test strategy
- [x] Architecture set: architecture, domain model, data model, security and tenancy, AI integration, integration patterns, platform and operations, design system, AIX pattern reuse
- [x] Twelve module spec pairs under `02-modules/` (functional and technical) written and cross-link-checked (`check_links.py`, 41 files, 2026-09-04)
- [x] Wireframes v3 (semantic colour, chips, selection bar, rows per page) dissected and adopted (ADR-18): Wireframes §8, Design System §8.1, skills updated, renders in `01-architecture/wireframes/v3/` (2026-09-05)
- [x] Wireframes v2 dissected and adopted (ADR-17): `01-architecture/WIREFRAMES.md`, renders in `01-architecture/wireframes/`, Design System §8, UX §11, frontend skills updated (2026-09-05)
- [x] Security definition of done in place: always-on rule, `xms-security-first` skill, Security Assurance control and evidence map, ADR-16, XA-05 (2026-09-05)
- [ ] Architecture sign-off (Matt) and Security ruling on ADR-02 requested
- [ ] Workbook reconciliation with DMS (110 vs 111 rows, 90 vs 75 Must Have) and regeneration of the register
- [x] Specs published to ClickUp Docs on the XMS list in X Platforms (list `901525777420`, 15 docs, 41 pages, map in [CLICKUP.md](./CLICKUP.md)), 2026-09-04

## 1. Foundations (`frontend/`, `backend/`, `infra/`, `feature/foundations-*`, Phase 1)

- [x] Frontend and backend scaffolds pushed 2026-09-07: `frontend/` to https://github.com/Mattb0124/xms-frontend (P1.1.2, P1.1.3, gate green: lint, typecheck, config check, 5 unit tests) and `backend/` to https://github.com/Mattb0124/xms-backend (P1.1.1, gate green: lint, typecheck, 5 unit and 2 e2e tests); each folder is its own git repository
- [~] Application folders (ADR-12, ADR-14): `frontend/` (Next.js, `styles/tokens`, `lib/axel-client`, `e2e`), `backend/` (NestJS with `src/domain`, `src/db`, `src/contracts`, `src/worker`, `test/kit`), `infra/` (Terraform, pipeline templates), `xms_mcp` module in `aix-mcp`; folders and READMEs created 2026-09-04, generic AIX skills seeded under `.claude/` and `.cursor/`
- [ ] Terraform: VPC use, ECS services, ECR repos, RDS PostgreSQL 16 with pgvector, S3 with GuardDuty Malware Protection, SQS queues and DLQs, SES identities and receipt rules, Secrets Manager, ALB and WAF, CloudWatch alarms
- [ ] Azure DevOps pipeline with the gate stage (lint, `tsc --noEmit`, unit, integration, isolation suite) before build; dev and production branch stages
- [ ] Dedicated XMS Clerk application: internal organisation with the THG IdP enterprise connection; `agents` JWT template; licensing and per-organisation SAML spike (Brayan)
- [x] API guard producing one `Principal` (Clerk JWT with authorizedParties and the agents audience, harness session token, API client, development token refused in production); global permission guard with realm separation; global `ValidationPipe`; route and permission snapshot (`backend/test/golden/routes.json`) and boot-time check; auth rejection suite, 2026-09-07 (backend `2088691`)
- [x] Data layer: SQL-first runner, `op`/`acct`/`sys`/`rpt` schemas, `sys.apply_account_isolation`, RLS session binding only in `withSession`, generated isolation suite (both roles, cross-account read/update/delete/insert) green in Testcontainers, 2026-09-07 (backend `0b64b37`)
- [x] Audit events append-only with trigger and the deferred audit guard on protected tables; `op.audit_events`; AuditService with diff, 2026-09-07
- [x] Security events written by the guard, bootstrap and every admin change (XA-01 done); usage events and telemetry ingestion done (XA-02); the event archive (XA-04): every stream and day to the object store as gzipped NDJSON of the digested rows with an append-only archive row, hourly behind the worker lease, listed under `/admin/integrity/archives`, 2026-09-07 (backend `ce0b5f5`, migration 0021); Parquet, Glue and partition detachment wait for the AWS environment
- [x] Admin: accounts with settings and status transitions, users with invite and the last-administrator rule, roles, reconciled grants and groups, configuration defaults seeded from JSON with versions and activation, `/v1/admin/me`, `POST /v1/bootstrap`; 82 integration tests, 2026-09-07 (backend `c0354fb`)
- [x] Account configuration overrides (P2.9.2 backend): `/v1/accounts/{id}/config/{kind}` describe, `PUT .../override`, `DELETE .../override` behind `admin:config`; validated like a default, audited on the account, the ticket service resolves the override for that account only; 5 scenarios green 2026-09-07 (backend `ac12ca6`); Configuration tab on the account record with the override editor, version history and remove flow, 2026-09-07 (frontend `4c794ba`); SLA policy bodies validated before activation and a null-default view, 2026-09-07 (backend `7840728`)
- [x] Frontend shell and house components (P1.4.1, P1.4.2): 32 components, finder bar, sidebar failing closed, palette, route registry, RTK plumbing, dev sign-in, telemetry client; 66 tests, 2026-09-07 (frontend `3c75b05`, `fbf630e`)
- [x] Outbox table and dispatcher (SKIP LOCKED, five attempts, dead letters), inbox and job lease tables, 2026-09-07
- [x] Ticket core (P1.5.1 to P1.5.6, P2.10.1 on the wall clock): tickets, SLA clocks with pause evidence, transitions with the close discipline, comments and work notes as separate tables, links, watchers, notifications with collapse keys, usage events and `POST /v1/telemetry`; 216 integration tests, 2026-09-07 (backend ticket core commit)
- [x] Frontend admin screens (P1.4.3, P1.4.4): accounts, settings, access, users, roles, groups, config viewer; 76 tests, 2026-09-07 (frontend `d2cee58`)
- [x] Frontend ticket screens (P1.5.5, P2.12.4 basics): Queue with system views and chips, New ticket, Ticket record with transitions and close discipline, My work, Dispatch, notifications bell; 99 tests, 2026-09-07 (frontend `d10b275`)
- [x] Portal API (P2.16 cut): `/v1/portal` me, submit, own requests, public timeline, comment, cancel and confirm closure; portal database role read-only, writes on the app role bound to one account, 2026-09-07 (backend `0986696`)
- [x] SLA breach sweeper and at-risk job with database leases (P2.10.2); saved views and the condition grammar (P2.11.1), 2026-09-07 (backend `0986696`)
- [x] Time entries, adjustments, buckets, contract periods, billing period locks, contract position (P2.12.2, P2.12.3, P2.13.1 cut, P2.13.2); 288 integration tests, 2026-09-07
- [x] Knowledge base (P2.14.1 to P2.14.4): articles with frozen versions, visibility sets, GLOBAL generalization with the identifier checklist, retrieval v1, Solutions rail, resolution record, portal knowledge search and client notes; 370 integration tests, 2026-09-07
- [x] Frontend portal screens (P2.16.3): own chrome, search-first home, requests, new request, request detail with the public thread, accessibility checks; 112 tests, 2026-09-07 (frontend `08315de`)
- [x] Attachments: presigned upload with size range and type condition (S3) or the signed local store (dev), MIME allowlist, scan gating with EICAR quarantine, portal public-only reads, 2026-09-07
- [x] Email: aliases, inbound pipeline (alias to account first, inbox dedupe, loop guard, thread matching, stripping, quarantine), outbound with typed templates, threading headers, plus-address replies, suppression and the SES event webhook; SES receipt rule to SQS pending the AWS environment, 2026-09-07
- [x] Frontend knowledge and time screens (P2.15.1, P2.15.2, P2.12.x): Solutions list and record, rail, Resolution and Time tabs, contract card, My timesheet; 135 tests, 2026-09-07 (frontend `9f28485`)
- [x] Frontend attachments, email panel, quarantine and alias screens (P1.6.2, P1.6.5); 150 tests, 2026-09-07 (frontend `9414f9c`)
- [x] Reporting (P2.19.1, P2.19.3, P2.19.4, P2.11.4, P2.11.5, P2.20.1 cut): measures, operations and account dashboards with view as client, portal dashboard, Excel and CSV exports, audit search over the unified events view, security and usage tiles, WSR on demand, nightly snapshots; 482 integration tests, 2026-09-07
- [x] Frontend dashboards, audit search, security and usage dashboards, exports, report packs, portal dashboard strip (P2.19.3, P2.11.4, P2.11.5, P2.19.4, P2.20.2 cut); 169 tests, 2026-09-07 (frontend `768b612`)
- [x] Axel adapter (P1.7.1 to P1.7.4, P2.17.1 to P2.17.3 cut): account AI switch with DPA and residency gates and the disable cascade, session tokens minted with the shared harness secret (the Clerk exchange cannot cross two Clerk applications; deviation in `session-token.service.ts`), SSE relay with cancel and thread index, redaction before egress, single-shot classify, prioritise, duplicate (trigram candidates until the embeddings endpoint), summarise and draft reply, thresholds and withheld rows, decisions applied through the ticket service with AI-actor audit, proposals route for the MCP `propose_*` tools, worker intake and expiry, accuracy with the what-if, operator defaults and kill switch, harness contract test; 551 integration tests, 2026-09-07 (backend `c1f1264`)
- [ ] Frontend Axel panel, suggestion cards, AI settings, accuracy and defaults screens: HELD 2026-09-07 (user: foundation first); the AI slice, SSE parser and `useAxelTurn` hook are on frontend main (`bd80d4f`), the panel draft is uncommitted in the working tree
- [ ] `xms_mcp` module in `aix-mcp`: HELD 2026-09-07 (user: not needed yet); draft stashed on `feature/xms-mcp` in aix-mcp (`git stash list`)
- [x] Tamper evidence (P1.7.6 cut): chained daily digests per stream with the digest file in the object store, `integrity.digest.written`, verification with `integrity.digest.mismatch`, nightly and weekly jobs, admin status, list and verify routes; Parquet export and Athena wait for AWS, 2026-09-07 (backend `4901317`)
- [x] Seed and demo data (P1.8.3): team across three groups, service user, two accounts with contrasting calendars and contract models, portal users, articles, tickets across every state with comments, notes, time and backdated clocks, AI switch on Brookfield; idempotent and under the time budget in the test, 2026-09-07 (backend `4901317`)
- [~] Hardening (P1.8.1): rate limiting on the portal, webhook and public routes with `abuse.rate_limited` done 2026-09-07 (backend `4184faa`); CORS allowlist and helmet in place; CSP report endpoint (`POST /v1/csp-report`, `abuse.csp_violation`) and the dependency audit script done 2026-09-07 (backend `6499fc8`); image-size through pptxgenjs has no patched release (exception recorded in WHAT-WAS-DONE); the ZAP baseline waits for the deployed hosts
- [x] Local run-through of the built API and worker tooling against the compose database with the seed; packaging gaps fixed (migrations and JSON assets in the build, tools run from dist, .env loader, blank values as unset, serialised client queries), 2026-09-07
- [x] Structured logging (P1.7.5 application side): pino JSON logger behind Nest, access line with request id and principal, redaction by path, 2026-09-07
- [ ] Harness changes 1, 5, 6 requested from the Axel owners (inline XMS agents, MCP server registration with `useCallerToken`, session token acceptance for the XMS tenant)
- [~] Observability: pino done; readiness endpoints done; OpenTelemetry through ADOT and alarms wait for the AWS environment (P1.7.5 infra side)
- [x] Tests: isolation suite (generated over every account-scoped table), auth rejection suite, outbox idempotency and dead letters, email threading corpus, harness contract, 2026-09-07 Frontend test count made truthful on 2026-09-09 (frontend `149ca25`): 57 files imported builders out of nine `redux/*Api.test.ts` modules, which re-ran those suites in every importer and inflated the count to 1744; the builders moved to suite-free `test-kit/*` modules and `test-kit/shared-fixtures.test.ts` fails when a test file imports another; the real count was 1013 before and is 1066 after the CP-03 web work.

## 2. Focused pilot (`feature/pilot-*`, Phase 2)

- [x] Ticket core: five types, configurable state machines, priority matrix, SLA engine on calendars with pause reasons and sweeper, links, comments and work notes, search and saved views, all built 2026-09-07 and recorded under section 1 on the ticket core line (backend `17a8dfd`), the sweeper and saved views line (backend `0986696`) and the frontend ticket screens line, with the calendar binding on the P3.26.1 line in section 3; the Queue reads the server's saved views since 2026-09-08 (frontend `ba6b516`): the view list shows them beside the system views, Save as view creates one from the current chips (the account chip supplies the required account, or the form asks), rename and delete for the owner, selecting one writes its conditions into the URL and names anything the chip grammar cannot express; group sharing waits on a group picker.
- [x] Time and contracts: entries, mandatory time before resolution through the close discipline, activity taxonomy and billable classes as configuration catalogs, four contract models (retainer, prepaid block, time and materials, fixed fee), burn-down and the per-ticket breakdown (`GET /v1/tickets/{key}/time` and the Time tab), 2026-09-07 (backend `db92891`); recorded under section 1 on the time entries line and the frontend knowledge and time screens line, with rate cards, forecast and billing periods on the Phase 3 line
- [x] Unlogged time and my week (P2.18.3 backend): `/v1/timesheets/me` and `/v1/timesheets/me/unlogged` from the person calendar and holidays with the eight-hour default, 2026-09-07 (backend); My timesheet rewritten over `/v1/timesheets/me` with the Time today card on My work, `/time` gated on `time:log`, 2026-09-07 (frontend `96e9014`); the response now carries the calendar block behind the expectation (backend `cc6e489`)
- [x] Roster (P2.12.1 cut, CAP-01): people, working calendars, skills catalog and levels, certifications, import from the sign-in directory, `capacity:view`; no rates, PTO or allocations yet; 8 scenarios green 2026-09-07 (backend); `/roster` list with URL filters and `/roster/{id}` with Details, Calendar, Skills and Certifications tabs, import button, assignee picker shows role and zone under `capacity:view`, 2026-09-07 (frontend `1093bee` to `b54bc3b`, suite 250 to 335); numeric fields now numbers on the wire (backend `cc6e489`)
- [x] Knowledge base: articles, versions, visibility, resolution record, similar solutions rail, portal search-first, 2026-09-07 (backend `8573bff`, frontend `9f28485`); recorded under section 1 on the knowledge base line and the frontend knowledge and time screens line
- [x] Portal (controlled access): threads, attachments and the consumption toggle (`consumption_visible` on the account settings gates `consumption_minutes` on the portal and the view-as-client dashboards) done 2026-09-07 (backend `0986696`, frontend `08315de`), recorded under section 1 on the Portal API line and the frontend portal screens line; the per-account request forms of CP-03 landed 2026-09-08 (backend `63329e3`, migration 0040; 7 scenarios): `acct.ticket_forms` and `acct.ticket_form_versions` hold one active form per account and ticket type, a draft the operator edits and a published version a trigger freezes, so the definition a request in flight answered cannot be rewritten; the twelve field kinds (short text, long text, choice, multi-choice, date, number, boolean, configuration item, contact, urgency, impact, attachment), required flags, conditions naming a field asked earlier, and a mapping to a ticket column or to `form_data` on the ticket all live in `src/domain/portal/form-schema.ts`, which decides the save and the submission alike. Authoring is `/v1/accounts/{id}/forms` with versions and publish under `admin:config`; the portal reads `/v1/portal/forms` and `/v1/portal/forms/{type}` and falls back to the fixed default, so the web form keeps working until an account publishes one; `POST /v1/portal/tickets` takes `answers`, refuses the old flat shape once a form is published (`form_answers_required`) and names every bad field (`invalid_submission`). The form builder screen is the web half and is not built The web half of CP-03 landed on 2026-09-09 (frontend `b5b5cf1`, `11428cc`): the request form builder on the account Configuration tab under `admin:config` (forms per type, a draft saved as one definition, publish behind a confirmation that the version freezes, the published version read only) and the portal request form rendering the published definition by kind with conditional visibility, submitting `{type, answers}` and wording every `invalid_submission` problem beside its field; the fixed form stays where nothing is published.
- [x] Email intake: thread matching (plus token, in-reply-to, references, subject key), reply-to-update as a comment on the matched ticket, attachment extraction through the allowlist and the scan gate, stripping, loop protection, quarantine, 2026-09-07 (backend `23976f4`); recorded under section 1 on the email line and the frontend attachments, email panel, quarantine and alias screens line; the `sharp` re-encode of inline images landed 2026-09-08 (backend `f786890`, migration 0041; 1 scenario plus 7 unit tests): every image that passes the allowlist is decoded and written out again before storage and before the scan gate, so the stored file is built from the pixels and carries none of the original container. The format is normalised (a JPEG stays a JPEG; PNG, GIF and WebP become PNG, with the stored name following), metadata is stripped, and the longest edge is capped at 4000 pixels, a limit this repository fixes because the specification set names none; the sizes and dimensions before and after sit on `acct.attachments.re_encode`, and an image that cannot be decoded goes to quarantine with `image_not_decodable` rather than being served on a guess. The same re-encode runs on a file a connector ingests, and since 2026-09-08 on a browser upload as well (backend `9dcb303`; 1 scenario): the presigned object is fetched back at confirm, decoded, written out again under the same key and only then scanned, with the row taking the new name, type, size and `re_encode` detail and an undecodable one quarantined as `image_not_decodable` exactly as an email image is, so no path now stores an image container this application never read
- [x] Reporting: internal and client dashboards with view as client, Excel and CSV exports, the basic WSR pack and nightly snapshots, 2026-09-07 (backend `9558c69`, frontend `768b612`); recorded under section 1 on the reporting line and the frontend dashboards line, with the PDF rendition, review before send and scheduled delivery on the Phase 3 line
- [~] Audit search, Security dashboard and Usage dashboard under Admin (XA-03): the three screens exist over `POST /v1/audit/search`, `POST /v1/audit/export`, `GET /v1/dashboards/security` and `GET /v1/dashboards/usage`; the backend half was completed on 2026-09-08 (backend `be6f881`, `3481382`, `5f3a0fc`; migration 0033; 3 scenarios). The search answers from all four event tables rather than three: migration 0033 unions `op.audit_events` into the `audit` stream of `rpt.events_v` with a null account and `attrs.scope = 'operator'`, so what an administrator does to a user, a role, a group or a configuration catalog is findable, and the search and the tamper-evidence digest now attest the same rows. It also binds to the grants: `sys.security_events` is an operator table with no policy of its own, so the service adds the clause the usage tiles already use and an `audit:read` holder bound to one account can no longer read another account's security rows. The Security dashboard gained the abuse events by kind, the clients the rate limiter turned away, what is paused across webhooks and connectors, the attachments the scanner held back and the open dead letters, each counted from the table that records it (`sys.security_events`, `acct.webhook_subscriptions.status`, `acct.connector_instances.kill_switch`, `acct.attachments.scan_state`, `sys.dead_letters`); neither pause table timestamps the pause, so that pair reports current state rather than a window count. The Usage dashboard gained `per_account`: tickets opened and closed, minutes logged, portal sign-ins, API client calls and active users, which needed the guard to stamp a portal sign-in with the one account it belongs to. Saved queries, the integrity panel on the Security screen, the core-loop funnel and the adoption measures in the catalog followed on the same day, recorded at the end of this line. The web half landed on 2026-09-08 (frontend `5c6c35d` to `e050172`, suite 1276 to 1296): the outbound queue depth on every connector health row, the operator stream in the audit search with a Portfolio account label and an Operator chip from the row scope, the rest of the Security dashboard (abuse by kind, rate-limited clients, paused integrations, quarantined files, open dead letters, each linking where a screen exists and the reader holds its permission) and the per-account strip on the Usage dashboard sorted by tickets created; both of the seams that line named are now closed on the backend, 2026-09-08. The audit condition grammar has `is_null` and `is_not_null` (backend `c16f7f0`; 5 unit and 1 integration scenario): they carry no value and take no bind parameter, so the offset numbering of the other conditions is untouched, and the web sends `{"conditions": [{"field": "account_id", "op": "is_null"}]}` for the Portfolio-wide filter and the same with `is_not_null` for its complement. Both are refused on a column every branch of `rpt.events_v` writes (`stream`, `event_type`, `occurred_at`, `actor_kind`, `outcome`) and refused when a value is sent with them, rather than ignoring it; `translateEvents` is the one place a condition is validated, so the search, the export and any saved query built on it accept the pair together (audit saved queries themselves are still unbuilt). The Security dashboard rows now name the record behind the count (backend `77745ef`; 1 integration scenario): a paused row carries `kind` (`webhook_subscription` or `connector_instance`), the row `id`, `account_id` and key, a display name (the endpoint URL or the instance name) and the reason, and a dead-letter row splits by queue, account and the `instance_id` its payload carries where the queue is a connector queue (`inbox` and `outbound` both write it), with the instance name joined where the reader can see the instance. The counts the current tiles read stay beside the lists as `paused_integrations_by_reason` and `open_dead_letters_by_queue`, and the `kind` vocabulary on both is the new one, so the web reads the totals from the new key. `sys.dead_letters` has no policy of its own, so the detail list binds to the grants the way the usage tiles and the audit search do, while the portfolio depth per queue stays operator wide. The web half of both changes remains. Saved queries landed on 2026-09-08 (backend `1b7bc74`, migration 0036; 3 scenarios): `op.audit_saved_queries` holds a named condition set per user with a `shared` flag, in the operator schema because a query carries no rows and no account, and what it may see is decided when it runs by the grant clause the search already applies, so two readers running one shared query each see their own accounts' events. The six routes under `/v1/audit/saved-queries` (list, create, read, `PATCH`, `DELETE` and `POST /{id}/run`) all stand on `audit:read`, the permission the search itself takes; sharing needs `audit:export`, because the specification names no permission for a shared query and that is the one already governing putting audit rows in front of other people. `translateEvents` validates the conditions on every save, so a saved query can never hold a condition the search would refuse, and running one is the inline search with the stored conditions rather than a second code path; a private query of another user answers 404 rather than 403, and editing or deleting is the owner's alone. The integrity panel followed (backend `cecce62`; 1 scenario): `GET /v1/dashboards/security/integrity` under `audit:read` answers `chain` (the last digested day per stream with its row count and hash, the last verification and its verdict, and the last mismatch, from `sys.event_digests` and `sys.digest_verifications`), `archive` (the last day exported per stream, when that export ran, and the days, rows and bytes in cold storage, aggregated from `sys.event_archives`), `streams` (rows per stream with the oldest and newest instants, read through `rpt.events_v` under the same grant clause the search applies, so the panel counts what this reader could open in the search) and `retention`, which is the policy of Audit & Analytics section 6 declared as a constant and carrying `detach_job_built: false`, because no table holds a retention promise and a screen must not read one as a measurement. The Usage dashboard gained the core loop and adoption (backend `c193b4b`; 2 scenarios): `funnel` counts the window's tickets at opened, first response, time logged, solution linked, resolved and closed, over the portfolio and again per account, each step from the record that proves it (`acct.tickets` for five of them, an existing `acct.time_entries` row for the logged time), and each step is counted on its own rather than as a subset of the one before, since a ticket can be resolved under a time exemption, so `drop_off` is the fall from the previous step and can be negative where a step was skipped; `adoption.by_role` reads the `action.completed` rows of `rpt.usage_events` with the role taken from `op.role_assignments` because the event never carries one, counting the users and events of the window beside `first_used_at`, the first time that role used that action at all, with an actor holding no role reported as `unassigned` rather than dropped. The role slice is not offered per account, because a role assignment is not a fact of the event, and the web half of all three remains. The web adopted the null operators as a Portfolio-wide account filter, the linkable paused-integration and dead-letter rows and the `_by_*` totals on 2026-09-08 (frontend `3bfea45`, `e2f3688`; suite 1325 to 1345). The web half of the remainder landed on 2026-09-08 (frontend `6a9e1d0`, `206abfc`, `ddb8c72`): saved queries on the audit search (save the builder's conditions, shared only for an `audit:export` holder, run through the run route with paging, rename and delete for the owner, the shared 404 worded), the integrity panel with never-verified streams on the warn trio and the retention months stated as policy, and the funnel strip with negative drop-off worded plus adoption by role; suite 1345 to 1401.
- [~] ServiceNow one-way ingest (P2.21.2, P2.21.3 cut): connector framework, instance onboarding, versioned maps with validation and activation, ingest-only mode, watermark poll, inbox apply with conflict policy, state map and journal comments, dead letters with replay and discard, kill switch, health with automatic trip, ticket sync card, in-process stand-in; 14 scenarios green 2026-09-07 (backend). Connector screens (P2.21.4) done 2026-09-07 (frontend `addd5a4`: health overview, add flow, instance record with settings, field and state map editors with validation, runs, dead letters, ticket Sync card; 242 tests); the Brookfield sandbox waits on the client facts (2026-09-25)
- [x] Axel assistive: categorise, prioritise, duplicates, summarise, similar solutions, HITL, thresholds, feedback (backend `c1f1264`; web pending the panel agent), 2026-09-07
- [~] Migration rehearsal import (P2.22.1 cut): batches, extract from a ServiceNow instance into raw rows and identity records, map through the active maps, load through the ticket service in import mode (no clocks, outbox or notifications; source dates and authors), reconcile counts per state and comments, explain deltas, four-eyes sign-off; 6 scenarios against the stand-in 2026-09-07 (backend). Migration console (P2.22.2) done 2026-09-07 (frontend `936c28a` to `495b94a`: batch list with URL filters, new batch, batch record with counts, log, records and record drawer, reconciliation with explain and sign-off; suite 335 to 421); names, `can_sign` and `sign_blocker` on batches and reports (backend `7840728`). Attachments, time records, the finance workbook, an asynchronous batch run, stable line ids and paging remain
- [~] Tests: state machine and SLA suites at 100 percent branch coverage done 2026-09-07 (domain 92 percent branch overall, 115 unit tests); Playwright golden paths green against the local stack with the seed (desk shell, portal home, security headers, ticket create, reply, work note, transition) 2026-09-07 (frontend e2e); the ZAP baseline waits for the deployed hosts

## 3. Operational replacement (`feature/ops-*`, Phase 3)

- [ ] Security-approved isolation in production; dedicated tier if ruled
- [ ] Portal SSO per account with fallback
- [x] Calendars and time zones driving SLA (P3.26.1): business calendar engine, holiday library, calendar documents with the account default and preview, SLA clocks bound to the calendar they started on, at-risk judged on working minutes, 2026-09-07 (backend); after-hours class on time entries (TB-13: derived from the account calendar and an optional local start, contract handling premium rate or comp time with the multiplier frozen on the row, `PATCH` contract handling, comp-time report) 2026-09-07 (backend `fd9ff20`, migration 0015); the web app's start-time field, class badge, contract handling editor and comp-time panel done 2026-09-07 (frontend `cac9ad2` to `07265cc`, suite 421 to 493); Calendars tab on the account record, calendar editor with the hours grid and preview panel, holiday libraries screen, 2026-09-07 (frontend `9318fe2`, `568fba0`); a repeated date in a posted library is now refused (backend `cc6e489`)
- [~] Rate cards, forecast, thresholds, adjustments, billing periods and export, finance connector, renewal alerts: rate cards as versions per effective date with the rate and amount frozen on the entry, forecast over business days from the account calendar, thresholds fired once per period with notification, outbox and audit, overage rule (block, flag, rate), rollover carry-over at period open, contract rules on create and patch, `GET /accounts/{id}/budget` and the drill-through, 2026-09-07 (backend `b5fffe3`, migration 0016; 6 scenarios); billing period lifecycle (submit with summary, reopen, approve, lock with the outbox event) and the finance export file as CSV or Excel with the checksum on the period and an append-only export record (TB-14), 2026-09-07 (backend `30c140c`, migration 0017; 4 scenarios); Budget tab on the account record (burn bar, forecast sentence, threshold markers, drill-through), rate card versions and the contract rules editor, amounts and the over-budget pill on time entries, 2026-09-07 (frontend `fcaf0db` to `987ed13`, suite 493 to 596); the auto-lock job done 2026-09-07 (backend `c93c327`); the finance connector delivery (INT-02) done 2026-09-08 (backend `b8215ce`, migration 0025); renewal alerts (INT-03) done 2026-09-08 (backend `f915f1b`, migration 0028): `acct.engagements` with the owner, renewal date, notice period and the status active, expiring or ended, contracts linked to one through a nullable `engagement_id`, routes under `contracts:view` to read and `contracts:manage` to write, and the daily `renewal.alerts` job that tells the owner and the account contract managers once per lead time of 90, 60 and 30 days and once at the notice-period boundary, records each firing on the engagement, writes an audit row and an `engagement.renewal_due` outbox event, and moves the status to expiring inside the window and ended past the date (10 scenarios); the billing period screens remain
- [~] Capacity: calculation, allocation grid, planned vs actual, overallocation, skills matrix. PTO by the person or a capacity manager (CAP-02), the month per person from the calendar, holidays, PTO, FTE and overhead with the status against the allocation (CAP-03), allocations as versioned grid cells (CAP-04), planned versus actual per person and account from logged time (CAP-05), the assignment-time check (CAP-06), read models rebuilt inline under the principal's account binding, 2026-09-07 (backend `7547439`, migration 0018; 4 scenarios); the certification expiry job done 2026-09-07 (backend `c93c327`); the skills matrix with single points of failure (CAP-07: people lens heat map, account lens over the technologies on active contracts with spof and gap flags) done 2026-09-07 (backend `583c268`, migration 0019); forward demand (CAP-08: pipeline per prospect or account with a probability, project as committed hours, CSV template import that refuses a file with any problem, weighted overlay on the month) done 2026-09-07 (backend `7b04942`, migration 0020); capacity screens done 2026-09-07 (frontend `83ee775` to `0d6e4a8`, suite 596 to 757: PTO tab, `/capacity` month grid with inline allocation cells and status pills, `/capacity/variance`, capacity markers in the assignee picker, Billing tab on the account record with the transitions and exports, Budget tab on `/accounts/{id}`); the skills matrix (`/capacity/skills`, both lenses, coverage chips on the account records) and forward demand (`/capacity/demand` with add, remove, CSV import, the overlay on the month grid) done 2026-09-08 (frontend `26b5304` to `3c4bc01`, suite 757 to 860)
- [~] Brookfield bidirectional sync with maps, loop prevention, conflict policy, comments and attachments, health and DLQ: built and green against the in-process ServiceNow stand-in 2026-09-08 (backend `3679482`, `fb71589`, `114e791`, `424c010`, `d863892`; migrations 0030 and 0031). `acct.sync_outbound` is filled from the outbox on the events the connector type declares, with the loop guard on `origin = sync:<instance>` and the work-note and internal-attachment refusals recorded as skipped runs; the deliver job claims per instance with SKIP LOCKED, translates fields through the active map in reverse and states through its outbound side, writes comments and work notes as journal entries carrying the correlation marker, the ticket key and the author, records the returned `sys_updated_on` and the value hash on the link as the echo guard, backs off and dead-letters into the existing dead letters with replay, counts towards the same health and automatic trip, and honours the kill switch on every claim and again mid-flight; the outbound conflict policy reuses the inbound vocabulary (`xms`, `external`, `newest`, `merge`, `external_at_create_then_xms`, `none`) with per-link overrides and records what was kept and dropped on the row, the run and the audit; attachments travel both ways inside the instance's limit through the scan gate. `GET /v1/connectors/{id}/outbound` and `POST /v1/connectors/{id}/outbound/{id}/retry`, the outbound state on the ticket Sync card, and `PATCH /v1/connectors/{id}` accepting `bidirectional` only with both maps active and a credential the instance has accepted. The web half landed on 2026-09-08 (frontend `0021e96` to `5245fe9`, suite 1145 to 1210): the Outbound tab on the connector record (event, ticket key, status pill, attempts, next attempt, last error, the conflict outcome with kept and dropped fields on expand, Retry on failed and dead-lettered rows, tab and status filter in the URL, gated on `admin:connectors` and covered by the fail-closed scan), the mode switch offering `bidirectional` with the blocker worded before the click and the server refusal beside it after, and the outbound state with a conflict note on the ticket Sync card; the health list renders outbound columns only where the route answers them, and both routes now do: `GET /v1/connectors/health` and `GET /v1/connectors/{id}` carry `pending_outbound` and `dead_lettered_outbound` next to the inbound depth, 2026-09-08 (backend `7aaf554`; 1 scenario), the dead-lettered count kept apart from `open_dead_letters` because the queue row and the `sys.dead_letters` row are two records of the same failure and the operator replays the second. The Brookfield sandbox still waits on the client facts (tables, fields, credentials, volumes, ownership); the webhook receiver, outbound record creation and the SQS FIFO transport remain
- [~] Public API and outbound webhooks (no register row of its own; the finance half of this line is INT-02, and the register's INT-05 is calendar integration, done below): API clients with scopes and account grants created by administrators (key shown once), webhook subscriptions per account for the public event catalog with the secret shown once, signed deliveries with retries, dead letters and the automatic pause, 2026-09-08 (backend `b7adcee`, migration 0024; 5 scenarios); the finance connector delivery (INT-02) followed on 2026-09-08 (backend `b8215ce`, migration 0025): one destination per account, an HTTPS signed post carrying the manifest or an object-store put under the prefix, acknowledgement through the API by a client with `exports:read`, supersession on re-delivery and the worker handler that delivers a period on lock; rate limits per API client done 2026-09-08 (backend `3d21d9c`, migration 0029): `op.api_clients.rate_limit_per_minute` (600 by default, set on create and returned on read) enforced by an interceptor that runs after the guard, so the counter keys on the resolved client id rather than on a string taken from the request, with `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `Retry-After` on the 429 and one `abuse.rate_limited` event per burst; the API client and webhook admin screens remain The webhook subscription admin screen cannot be built yet: `/v1/webhooks` answers only an API client principal (an admin bearer is pinned to 404 in `test/webhooks.int-spec.ts`) and there are no pause, resume, dead-letter or replay routes, so it waits on session-principal routes under `webhooks:manage` (recorded 2026-09-09).
- [~] Scheduled report delivery, snapshots, CSAT: report schedules per account (cadence, run day and time in the account zone, period kind, distribution list) with the next run computed in the zone, run now, the run history with the per-recipient outcome, delivery as a notification with the download for internal recipients and an email with the link from the account sender identity for contacts and portal users, and the worker runner that claims due schedules behind its lease, 2026-09-08 (backend `25b1e3a`, migration 0022; 4 scenarios); snapshots were already nightly; CSAT on ticket close (one survey per closed ticket and requester unless suppressed, the prompt with a one-time link, portal and link answers, reminder and expiry tick, low scores to the contract managers, the per-account summary) 2026-09-08 (backend `bf902f2`, migration 0023; 4 scenarios); the survey and schedule screens landed on 2026-09-08 (frontend `a497cca` to `2f4c969`, suite 860 to 898): `/portal/surveys` with the token page, the Satisfaction tab on `/accounts/{id}` and the Report packs tab on the admin account record; the quarterly relationship survey (CP-07 remainder) done 2026-09-08 (backend `3eb302e`, migration 0032; 5 scenarios): the `portal.csat.quarterly` job creates one survey per period and recipient on or after the first business day following a quarter end, taking the business day from the account calendar where there is one and from weekdays otherwise, for the portal users holding the account-admin role plus the contacts flagged `executive_sponsor` (the flag is a new `acct.contacts.flags` column on a closed vocabulary with a thin admin route under `admin:accounts`), with five keyed questions plus a comment, two reminders at day seven and day fourteen and expiry at day twenty-one, the answer routes taking either shape according to the survey's kind, a quarterly block on the account summary (latest period, averages per question, the trend over four periods) and both kinds in the portal list; the quarterly survey screens landed on 2026-09-08 (frontend `9f959bd`, `bda8343`): the portal list shows both kinds with the period, the answer form and the `#token=` page render the five keyed questions or the single score from the survey's own questions (the link page has no read route, so it opens on the one question and, on the server's `scores_required` refusal, asks the five it names), and the account Satisfaction tab carries the quarterly block (latest period, averages per question, the four-period trend) only where the API answers it; the PDF rendition done 2026-09-08 (backend `71617d7`; 4 scenarios): a run now stores both renditions from one description of the pack, so the numbers cannot disagree, rendered with `pdfkit` and the standard Helvetica faces rather than a headless browser, one cover plus one page per section with "No activity this period" where a section is empty, and `GET /v1/reports/packs/{id}?format=pdf` mints the document link beside the deck one; review-before-send done 2026-09-08 (backend `b027973`, migration 0034; 4 scenarios): a schedule with `review_required` holds its run at `ready_for_review` with the deadline its grace period gives it and notifies the account's active `reports:manage` holders plus administrators, `GET /v1/reporting/runs/{id}` answers the run with its frozen pack and a link to each rendition, `POST .../approve` delivers from the pack that was held and `POST .../cancel` records the reason and sends nothing (both `reports:manage`, anything not held is a typed 409), the `report.review.deadline` tick moves an unreviewed run to `awaiting_review` and reminds the reviewers rather than releasing it, because unreviewed narrative never reaches a client, and every move writes its audit row and outbox event in the same transaction; the read behind the survey link done 2026-09-08 (backend `e10ba95`; 4 scenarios): `POST /v1/csat/{id}/describe` answers the survey's kind, period, ticket key where there is one, status, expiry and the questions of its kind behind the same one-time token the answer route takes, both routes now sharing one gate so the account resolution, the constant-time hash comparison and the 404 cannot drift apart, which is what lets the link page render the five keyed questions or the single score instead of opening on the one question and learning the shape from the server's refusal; it says nothing about the person the survey went to (no account id, no contact), and an unknown id and a token that does not match answer the same 404, so the route never confirms an id. The portal survey list no longer carries `token_hash` (backend `63874f2`; 1 scenario): `select s.*` had been serving the sha256 of every recipient's link token to a browser, and the query now names its columns behind a `PublicSurveyRow` type that is the row minus the hash. Delivery links live the fourteen days functional 5.7 promises rather than the seven they were minted for (backend `f68df97`), kept in one named constant that the delivery email words its number from, so the link and what the client was told cannot disagree, and asserted on the renditions of a held run. The narrative editor landed on 2026-09-08 (backend `c0669cd`, migration 0035; 3 scenarios): `PATCH /v1/reporting/runs/{id}/narrative` takes the prose section by section, keyed to the sections of the pack itself (`headline`, `service_levels`, `backlog`, `consumption`), and lands it as one more entry on `narrative_versions` naming the editor, with the pack's source becoming `edited` (migration 0035 widens that column's vocabulary; `axel` stays unreachable while `wsr_narrative` has no capability builder, so a pack is templated until somebody rewrites it, which is what functional 5.8 and AI functionality 117 ask the screen to say). Only the prose moves: the measures and the notable rows were frozen when the run rendered, and a review that could move a number would not be a review. `POST /v1/reporting/runs/{id}/regenerate` is "Regenerate with my edits": both renditions again from those frozen numbers and the narrative the pack now carries, replaced under the run's own keys with fresh links because the old ones were signed against the objects as they were, and the run does not move (same status, same deadline, same reviewer). Approve rebuilds first where an edit was never regenerated, so the words the reviewer wrote are the words that ship whichever button they press, and "Send without changes" is the same route on a pack nobody edited. `GET /v1/reporting/runs/{id}` answers the narrative, `narrative_source` (`templated`, `ai` or `edited`), its version, whether it has been rendered and whether Axel is on for the account. Both write routes are `reports:manage`, anything not held is the same typed 409, and the assertions are on the text of the stored PDF rather than on the row, because what a client receives is the only thing that settles whose words were sent; the review screen itself remains The review screen landed on 2026-09-08 (frontend `80538c5` to `4450a87`, suite 1296 to 1325): `/reports/runs/{id}` under `reports:manage` with the run status and deadline, the pack sections rebuilt from the frozen measures, a link per rendition including the PDF, Approve and send, and Cancel with a required reason; the review pill on the run history, the review-required switch on the schedule form, and the reviewer notification and waiting row opening the screen through safe links. The editable narrative and Regenerate with my edits follow in the next web pass now that the routes exist. The survey link page reads `POST /v1/csat/{id}/describe` before asking anything (frontend `1af0731`): the local question text and the refusal probe are gone, an invalid link is one worded state, an answered or expired survey shows its status. The narrative panel on the review screen is editable per section with Regenerate with my edits, Approve and send, Send without changes and the AI-off sentence (frontend `e83f662`). Cancel now refuses a missing, empty or blank reason with one typed `400 { code: 'reason_required' }` from the service rather than the validator's generic message for one shape and silence for the others, and stores the trimmed text (backend `ccd2624`, 2026-09-08); the Report packs row of "Waiting on me" opens `/reports/runs/{id}` of the newest held run for the reader's accounts instead of a tab the reviewer had to search again, keeping the account's Report packs tab where runs wait on more than one account and the packs screen where none wait (backend `5f9fae4`, 2026-09-08).
- [x] "Waiting on me" links the web application serves, 2026-09-08 (backend `46c891e`): every row of `GET /v1/me/waiting` now answers an address from `frontend/lib/routes.ts` written in that screen's own URL grammar (`/tickets?view=mine`, `/tickets?out_of_scope=flagged` for the out-of-scope flags since the Queue gained that parameter in `dfb72ab`, `/knowledge?status=in_review`, the account record's Report packs tab, `/time`, the account dashboard's Satisfaction tab) instead of the `/queue`, `/timesheet`, `/reports/runs` and `/notifications` space of its own the browser had to translate; the two account-scoped rows name the account of the newest waiting record and fall back to the list screen when nothing waits, and unread notifications carry no link because the bell is in the shell on every page (9 scenarios); the web rail follows those addresses since 2026-09-08 (frontend `e341ad5`): the API's link is used where present and the local map is only the fallback; suite 1210 to 1276; the Report packs row went on to open the held run itself at `/reports/runs/{id}`, which the application does serve, on 2026-09-08 (backend `5f9fae4`)
- [ ] Axel: draft replies, WSR narrative, time assistance, burn anomaly; harness changes 2 to 4
- [ ] Full migration, reconciliation sign-off, parallel run, cutover, decommission
- [ ] Availability and recovery objectives met; runbooks rehearsed

## 4. Later releases (Phase 4)

- [x] Out-of-scope flag workflow (TM-11), 2026-09-08 (backend `1af1e82`): `POST /v1/tickets/{key}/scope` flags the work with a reason under `tickets:work`, or withdraws a flag nobody has decided; the reason, who raised it and when live in `out_of_scope_detail`, so `out_of_scope` stays the closed vocabulary migration 0004 gave it and the queue and the waiting rail keep counting it; the flag is audited, written to the outbox as `ticket.scope_flagged` and notified to the account's contract managers, who are the people who decide what the client is entitled to. `POST /v1/tickets/{key}/scope/decision` decides it under `tickets:approve-scope` and never for the person who raised the flag (technical 3.3): an approval may carry an allowance in minutes, which lands on the current contract period's `carried_over_minutes` (the period has no separate overage column, and that field is exactly the extra minutes the burn maths adds to the contracted figure) with a second audit row naming it an out-of-scope allowance so it is never read as an ordinary rollover, and a decline ends the flag and words why; the ticket view carries `scope` with the flag, the reason, both actors, both instants, the decision, its note and the allowance (6 scenarios). The Queue had no way to ask for the flagged tickets when this landed, so the waiting row opened the Queue and the count carried the meaning; the filter below closed that. The Scope card on the ticket record landed on 2026-09-08 (frontend `175f88b`): the flag state, reason, both actors and instants, the decision, its note and the allowance; Flag out of scope and Withdraw under `tickets:work`, Approve with an optional allowance and Decline with a note under `tickets:approve-scope`, hidden from the flagger, every refusal worded. The Contacts tab on the admin account record (frontend `d343970`) lists contacts with their flags and sets `executive_sponsor` and the rest of the vocabulary under `admin:accounts`, covered by the fail-closed scan. The Queue filters on the flag since 2026-09-08 (backend `dfb72ab`; 3 scenarios): `GET /v1/tickets?out_of_scope=flagged` takes one or more values of the closed vocabulary migration 0004 declares and refuses anything else before the service, `out_of_scope` joins the condition allowlist as an enum so a saved view carries it with `eq`, `in` and their negations, and the vocabulary is written once in `conditions.ts` beside the other field vocabularies with the query DTO reading it from there, so the parameter and the grammar cannot drift apart; the waiting rail row now opens the tickets it counts rather than the whole Queue. The chip itself is the web half and remains The Queue gained the "Flagged out of scope" system view and the out-of-scope chip dimension on 2026-09-08 (frontend `d506609`), and the waiting rail follows `/tickets?out_of_scope=flagged`.
- [~] Groups and change windows, PTO and holidays, non-ticket time, pipeline demand, forecasting, additional dashboards, all Nice to Have rows per the register. PTO and holidays (CAP-02), pipeline demand (CAP-08) and the forecast landed with capacity and budget in Phase 3. Assignment groups (TM-08) done 2026-09-08 (backend `0979d95`, migration 0037; 8 scenarios): the catalog and its membership were already operator-scoped rows and the ticket already carried a group, so what landed is everything that makes a group a queue rather than a column. `acct.group_routing_rules` is the account's answer to which group takes which kind of work, (type, category) to group, account-scoped with forced RLS because it is a per-account override of an operator default, the rule naming a category winning over the rule for the type as a whole, a retired group routing nothing, and the whole set reconciled with one PUT under `admin:config` while reads sit under `tickets:view` because the New ticket form shows where work will land; a ticket created without a group is dispatched by the matching rule and what the caller asks for still wins. Assigning a group is now its own audit row and outbox event, `ticket.group_assigned`, on create and on patch, the create payload saying whether the desk chose the group or the routing default did. The group queue is `GET /v1/tickets?my_groups=true` and the `is_mine` operator on `group_id` in the condition grammar so a saved view carries it, the groups read from `op.group_members` on the server and never named by the client, and a person in no group getting an empty queue rather than an unfiltered one; saved views shared with a group are now visible to that group's members, which `share = group` never was, because `ViewsService` resolved the member's groups as an empty list. Removing a member answers with the open tickets they still hold, listed under the administrator's own account binding, and does not block the removal, because a person may leave a team mid-flight. Change windows (TM-10, TM-18) done 2026-09-08 (backend `074d39e`, migration 0038; 7 integration and 5 unit scenarios): `acct.ticket_groups` is the named container with a schedule that a ticket tree belongs to, the schedule an explicit start and end plus freeze windows exactly as functional 5.9 words it, so there is no recurrence column and a repeating window is repeated records; membership is the ticket's own `ticket_group_id`, which now gains its foreign key, and the data model's `acct.ticket_group_members` join table is not created, because a ticket belongs to at most one group and two records of one fact can disagree. A Change cannot reach Scheduled unless it belongs to a change window with both ends, the requirement the change machine already declared and the close-discipline check now answers; scheduling into a freeze window, or onto a configuration item another change already holds in an overlapping window, is refused as `change_freeze` or `change_conflict` naming what is in the way, and `change_window_reason` acknowledges it and lands on the audit as `ticket.change_window_acknowledged`, which is the warning-with-a-reason functional 5.13 asks for; the state the machine marks `effects.deploy` may only be entered inside the window and outside every freeze, and outside it the transition is refused as `outside_change_window` with only `tickets:override-change-window` plus a reason getting through, audited as `ticket.change_window_overridden`. The deploying state is data on the machine rather than a state key written into the service, so an account whose machine names its states differently can say which one touches production; the seed carries the flag and the migration sets it on the machines already stored. Routes: `GET/POST /v1/ticket-groups` and `GET/PATCH /v1/ticket-groups/{id}` (`tickets:view` to read, `tickets:work` to write), `GET /v1/change-calendar` for a range and `GET /v1/change-calendar/at` for whether an instant is inside a window, both `tickets:view` and both answering from the same pure rules the transition gate uses, so a screen and the server cannot disagree. The specification names the Scheduled gate and the freeze calendar but not the implementation gate, so that one is the default the brief asks for, worded as a 409 carrying the window, its ends, the freeze and the permission that would carry it; the TM-11 out-of-scope freeze is not tied here, because nothing in the specification connects a flagged ticket to a change window. Non-ticket time (TB-12) done 2026-09-08 (backend `95ed324`, migration 0039; 6 scenarios): buckets have existed since migration 0006 with a key, a label and a billable class, and what landed is the `code` on the closed vocabulary (governance, qbr_prep, account_mgmt, escalation, custom) so reporting groups the same kind of work across accounts whatever a client's bucket is called, and a nullable `contract_id` so a bucket says which contract its time belongs to instead of always falling back to the account's single active one. A bucket that names no billable class takes the first class the account has that does not consume the contract, the "classed Internal" default of functional 5.9, and whether an entry burns the contract follows that class exactly as a ticket entry does, so the burn maths needed no ticket-versus-bucket rule: a non-billable bucket lands in `non_consuming_minutes` and moves nothing else, a billable one consumes the period, which is what lets the client portal show the buckets by name under Detailed consumption. The log route now sits where technical 4 puts it, `POST /v1/accounts/{id}/buckets/{bucketId}/time-entries`, and checks the bucket belongs to the account in the path, a parameter that was previously read and ignored; a bucket entry writes the `time.logged` outbox event that only ticket entries were writing, so capacity's read model and the reporting snapshots see all of the time and not some of it; `PATCH` on a bucket renames it, moves its class or retires it under `contracts:manage`. The timesheet and the capacity actuals needed nothing added, because both already read every entry of the person whatever it hangs off, and the tests now pin that so it cannot regress; the close discipline stays ticket-only by construction, because the minutes it counts are the minutes on the ticket. Additional dashboards and the remaining Nice to Have rows are still open, and all three of these are backend only: the web halves remain
- [x] Bulk actions (TM-16), 2026-09-08 (backend `2b0420d`; 6 scenarios): `POST /v1/tickets/bulk` takes one action and up to a hundred ticket keys, each carrying the version the caller read, and answers one outcome per key: `ok` with the resulting version, `version_conflict` with the version to reload, `refused` with the typed code the single-ticket route would have answered, or `not_found`. The seven actions are assign, set group, set priority, transition, close, comment and work note, and every one of them runs the ticket through the existing `TicketsService` path, so the audit entries, outbox events, SLA clocks, close discipline, change-window gate and watcher notifications are the ones the record screen produces rather than a second set written for the batch; a bulk transition into a frozen change window is refused as `change_freeze` naming the freeze, and a bulk close without its resolution as `missing_requirements` naming the items, exactly as they are one ticket at a time. The request is deliberately not one transaction: each ticket runs in its own unit of work, so a refusal on the fourth ticket leaves the first three applied and the fourth untouched, there is no partial write inside one ticket, and the answer to a mixed batch is the report rather than a rollback of work the desk has already seen succeed. The route declares `tickets:work`, the lowest permission any of the actions needs, and the service refuses the whole batch with a 403 naming the permission when the caller lacks the action's own one (`tickets:override-priority` for a priority, `tickets:resolve` for a close), because a half-applied batch is not a permission decision; the hundred-key bound is refused as `too_many_tickets` before a ticket is touched, and a repeated key is applied once so a double-click cannot double-post. The comment and work-note routes take no version of their own, so the version the caller read is checked inside the same transaction as the write rather than being quietly ignored on a bulk call. The selection bar on the Queue still loops one call per ticket, so the web half remains
- [x] Configuration items (TM-19), 2026-09-08 (backend `ec69c7b`; 4 scenarios, no migration): `acct.configuration_items` has existed since migration 0007 with every column the Knowledge Base technical specification 2.6 names, and two readers already depended on it (the change-conflict rule `conflictsOnItem`, and the generalization checklist that scrubs an article of the account's own hostnames), but nothing could put anything in it. `GET` and `POST /v1/accounts/{id}/configuration-items` and `GET`, `PATCH` and `DELETE /v1/configuration-items/{id}` close that: reading sits under `tickets:view`, because the New ticket form and the ticket record show the picker to everyone who works tickets, and writing under `admin:config`, where the account's other vocabularies (catalogs, calendars, routing rules) already live. The list route is the picker's search: `q` matches anywhere in the name, active items sort first so a retired one never heads the list, and `ci_type` and `status` narrow it; the item record carries its own open and recently closed tickets. An item a ticket or a `ci_link` still names is refused on delete as `configuration_item_in_use`, because a dangling id would leave the change-conflict rule silently refusing nothing, and retiring it is how an item with history leaves service. The ticket create and patch paths now take `configuration_item_id` and assert in the same transaction that it belongs to that ticket's account, an id row-level security hides answering not-found rather than forbidden, which is the check `src/domain/portal/form-schema.ts` was waiting for: a `ci_picker` answer on a per-account request form now maps onto the ticket column instead of being kept as an answer, and the ticket view carries the item. The portal-side "account admins may add items" of functional 5.8 is not built, because no portal screen asks for it; the register screen and the picker are the web half and remain
- [x] Calendar integration (INT-05), 2026-09-08 (backend `a6a7fa8`, migration 0042; 3 integration and 6 unit scenarios): the register asks for change windows and scheduled work on Outlook calendars, and the specification's shape for it is a Microsoft Graph push per consenting user, which needs an application registration, a consent flow and stored refresh tokens. What landed is a subscribed ICS feed, which reaches the same Outlook, Google and Apple calendars with none of that and leaves XMS holding nobody's credential; the Graph push stays open, and this is the half that can be built and operated today. `GET /v1/accounts/{id}/change-calendar.ics` under `tickets:view` answers a signed-in reader one account, and `GET /v1/calendar-feed/{id}/change-calendar.ics?token=...` answers a calendar client, which cannot carry a bearer header: that route is public with its reason declared and joins the public rate-limit policy beside the CSAT survey link. `POST` and `DELETE /v1/me/calendar-token` mint and revoke the feed, the token answered once and stored only as its SHA-256 in `op.calendar_feed_tokens`. That is the CSAT one-time-link scheme already reviewed here: the id in the path names the row so the lookup is a primary key read rather than a search on a secret-shaped column, the presented token is compared in constant time, and an unknown id and a wrong token answer the same 404, so the route never confirms an id. The token feed covers every account its owner is granted, resolved fresh through the guard's own `resolveAccess` on every read so a revoked grant, a deactivated user or a suspended account changes the feed at the next poll, and narrows to one client with `account_id`. The writer in `src/domain/integrations/ics.ts` is pure and unit tested on constructed windows: every stamp is UTC with the trailing `Z`, so there is no `VTIMEZONE` block and each reader renders the window in its own zone as functional 5.6 asks; `UID` is stable per window and per freeze, so a re-read updates the event a subscriber already holds instead of adding a second one; `SEQUENCE` is the record's version less one, so an edit is the higher number; a freeze is written `TRANSPARENT` because it is a rule and not an appointment; and a cancelled window stays in the feed as `CANCELLED` so a subscriber can be rid of it. Scheduled work from the allocations grid, and the per-user Graph push itself, both remain The web halves landed on 2026-09-09 (frontend `6789cd9` to `9fd9fb6`): the bucket log call moved to the renamed `/time-entries` route (the rename had broken it), the My groups chip and the group dimension on the Queue, the group on the ticket record with a picker that also serves share-with-group on saved views, routing defaults on the Configuration tab, the ticket-group catalog at `/tickets/groups`, the change calendar with freezes and the window rules worded on a transition with the override for its permission holders, and non-ticket time on the timesheet with the buckets panel on the account Contracts tab; the suite count reported as 1744 is inflated by test files importing builders from other test files, which the next web pass moves into test-kit.

## Decisions locked

- Standalone product, Option B (ADR-01); PostgreSQL with forced RLS and a dedicated tier (ADR-02); one XMS Clerk application for both populations pending the spike (ADR-03); Axel only, through the adapter, XMS as an MCP server (ADR-04); knowledge base first-class, resolution is a record (ADR-05); one connector framework (ADR-06); XMS POC ported, studio module retired (ADR-07); TypeScript worker sharing domain code (ADR-08); gates in the pipeline and Terraform in the repo (ADR-09); operator-wide CS keys (ADR-10); report packs rendered in XMS with an Axel narrative (ADR-11); separate repositories `frontend`, `backend`, `infra` plus `xms_mcp` in `aix-mcp`, no monorepo (ADR-12); the apps live in this repository as `frontend/`, `backend/`, `infra/` (ADR-14); pilot-grade core built in one month with scope cuts, not date moves (ADR-15).

## Blocked / open

- Path decision (Ignacio, 2026-09-07): building for Path 1 by default.
- Isolation ruling (Juan / Security, 2026-09-18): shared tables with forced RLS plus dedicated tier by default.
- Pilot subset and workbook reconciliation (Sofi / DMS, 2026-09-18): Appendix C and this register's phase column by default.
- Brookfield facts (Vini, 2026-09-25): ingest-only first, Table API with OAuth client credentials by default.
- Harness changes and guarantees (Axel owners, 2026-10-15): items 1, 5, 6 in Phase 1; 2 to 4 by Phase 3. Built against XT_AXEL_API.md with a fake harness and a contract test; the live smoke waits on the XMS agents and the shared session secret.
- Worker batch identity: intake calls the harness as a synthetic `axel-service` principal (single-shot facts travel in the message, so no MCP tool call needs it); a real service user with account grants is needed before any tool-using batch face (P1.8.3 seed).
- AV product and environments (Infrastructure, 2026-09-25): GuardDuty by default.
- Clerk licensing and SAML topology (Brayan, 2026-10-02): works within plan by default; XMS-owned realm as fallback.
